spring.application.name=protime

# TODO colocar variaveis em outro arquivo para que sejam puxadas para cá. Assim não manipularão diretamente este arquivo
# Endereço MySQL
spring.datasource.url=jbdc:mysql://localhost:3306/protime-back?serverTimezone=UTC

# username
spring.datasource.username=root

# password
spring.datasource.password=root

# TODO verificar se tem como ele criar uma vez, e depois não criar mais
# estrategia de criacao
spring.jpa.hibernate.ddl-auto=create

# TODO comentar antes da finalização, para que o console fique apresentavel.
# mostrar no console o que está acontecendo
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
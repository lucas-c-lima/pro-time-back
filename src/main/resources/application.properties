spring.application.name=protime

# TODO colocar variaveis em outro arquivo para que sejam puxadas para ca. Assim nao manipularao diretamente este arquivo
# Endereco MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/sistema_gerenciamento?serverTimezone=UTC

# Username
spring.datasource.username=root

# Password
spring.datasource.password=root

# TODO verificar se tem como ele criar uma vez, e depois nao criar mais
# estrategia de criacao
spring.jpa.hibernate.ddl-auto=update

# TODO comentar antes da finalizacao, para que o console fique apresentavel.
# mostrar no console o que esta acontecendo
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

api.security.token.secret=${JWT_SECRET:my-secret-key}